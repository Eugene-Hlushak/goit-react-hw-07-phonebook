export const getContacts = state => state.contacts.items;
export const getContactInfo = state => state.contacts.contact;
export const getLoading = state => state.contacts.isLoad;

export const getStatusFilter = state => state.filter;
